namespace AutogenRundown.DataBlocks.Enums;

/// <summary>
/// Level tiers. We have these map directly on to the games tier values for compatability
/// </summary>
public enum Tier
{
    A = eRundownTier.TierA,
    B = eRundownTier.TierB,
    C = eRundownTier.TierC,
    D = eRundownTier.TierD,
    E = eRundownTier.TierE
}
